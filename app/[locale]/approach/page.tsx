import { getContent } from '@/lib/content';
export default async function Approach({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=getContent(locale);return <><section className="page-hero"><div className="container"><div className="eyebrow">APPROACH</div><h1>Assess. <span>Engineer.</span> Optimize.</h1><p>{t.pages.approach.intro}</p></div></section><section className="section section-dark"><div className="container"><div className="method-grid">{t.method.steps.map(([n,h,p])=><div className="method-step" key={n}><span>{n}</span><h3>{h}</h3><p>{p}</p></div>)}</div></div></section></>}
