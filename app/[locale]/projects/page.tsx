import Link from 'next/link';
import { getContent } from '@/lib/content';
import { localeAlternates } from '@/lib/seo';
export async function generateMetadata({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=getContent(locale);return {title:t.pages.projects.title,description:t.pages.projects.intro,alternates:localeAlternates(locale,'/projects')}}
export default async function Projects({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=getContent(locale);return <><section className="page-hero"><div className="container"><div className="eyebrow">PROJECTS</div><h1>Show the work. <span>Prove the outcome.</span></h1><p>{t.pages.projects.intro}</p></div></section><section className="section"><div className="container"><div className="listing-grid"><article className="listing-card"><b>CASE STUDY 01</b><h3>Legacy modernization</h3><p>A future-ready structure for documenting the challenge, engineering approach, implementation and measurable outcome.</p></article><article className="listing-card"><b>CASE STUDY 02</b><h3>Connected operations</h3><p>A future-ready structure for showing how PLC, SCADA, IIoT and data are connected into an operational system.</p></article></div><div style={{marginTop:50}}><Link className="button" href={`/${locale}/contact`}>{t.headerCta}<span>↗</span></Link></div></div></section></>}
