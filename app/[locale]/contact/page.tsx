import { getContent, type Locale } from '@/lib/content';
import ContactForm from '@/components/ContactForm';
export async function generateMetadata({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=getContent(locale);return {title:t.pages.contact.title,description:t.pages.contact.intro,alternates:{canonical:`/${locale}/contact`,languages:{en:'/en/contact',fr:'/fr/contact'}}}}
export default async function Contact({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const L=locale as Locale;const t=getContent(locale);return <><section className="page-hero"><div className="container"><div className="eyebrow">CONTACT</div><h1>{t.pages.contact.title}<span>.</span></h1><p>{t.pages.contact.intro}</p></div></section><section className="section"><div className="container contact-grid"><div className="contact-info"><div className="eyebrow">START WITH THE PROBLEM</div><h2>Tell us what needs to work better.</h2><p>{t.cta.text}</p><p><strong>Royal City Labs</strong><br/>Canadian industrial engineering, automation, IIoT and optimization.</p></div><ContactForm locale={L}/></div></section></>}
