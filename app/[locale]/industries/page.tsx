import { getContent } from '@/lib/content';
import Reveal from '@/components/Reveal';
export default async function Industries({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const t=getContent(locale);return <><section className="page-hero"><div className="container"><div className="eyebrow">INDUSTRIES</div><h1>Engineering <span>adapted</span> to the plant.</h1><p>{t.pages.industries.intro}</p></div></section><section className="section"><div className="container"><div className="listing-grid">{t.industries.items.map((x,i)=><Reveal key={x}><article className="listing-card"><b>{String(i+1).padStart(2,'0')}</b><h3>{x}</h3><p>Automation, process control, integration, data and optimization adapted to the operating context.</p></article></Reveal>)}</div></div></section></>}
